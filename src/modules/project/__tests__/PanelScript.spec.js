// import TestCaseSetup from '../../../utils/test/Setup.js'
// import PanelScript from '../PanelScript.vue'
describe('@/modules/project/PanelScript.vue', () => {
    it('normal use', async () => {
        // let setup = new TestCaseSetup();
        // await setup.setup();
        // await setup.setActiveProject('new');

        // let wrapper = setup.mount(PanelScript);
        // await Setup.msleep(1000);
        
        // // let inputName = wrapper.findComponent({ref:'inputName'}).find('input');
        // // expect(inputName.element.value).toBe('TEST PROJECT');
    })
});